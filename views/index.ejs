<% include header %>
<% if(foods.length){ %>
	<% foods.forEach(function(username){  %>
	<div class="panel panel-default">
		<div class="panel-heading">
			<div class="text-muted pull-right">
				<%= username.published %>
			</div>
			<%= username.food.title %>
		</div>
	</div>
	<% }) %>
	<% } else { %>
	No entries! <a href="/new-entry"> Add a Favorite Food?</a>
	<% } %>
<% include footer %>
